var number int = 10
//This is a single line commnet which will be ignored

/* 
 And this is a multiple line
 comment that will also get
 ignored
*/

//Initialize variables
var name string = "Gabriel"
var lastName string = "Igliozzi"

//Create function that will add both strings and return it
func addNames(str1 string, str2 string) string {
	return str1 + " " + str2
}

print(addNames(name, lastName))

const result string = addNames(name, lastName)
//result = "sapo"

name = "hols"

if identifer == "hola" {
    var isTrue bool = false
    
    if false {
        const isTrue_ bool = true
    }
}

//Exmaple of the fibonacci sequence in mar


//Recursive function to calculate the fibonacci sequence
//Using a seed input (int)
func fibonacci(num int) int {
    if num <= 1 {
        return num
    }

    return fibonacci(num - 1) + fibonacci(num - 2)
}

//Const to mark the end of the number we want to get
const limitNumber int = 100

//Print the result
print(fibonacci(limitNumber))



