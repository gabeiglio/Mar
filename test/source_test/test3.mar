/* This file will be intendent to test the following:
	* Statetemnts like if, while 
	* Declarations like var, const, func, class
	* calls, expressions, nested stuff
*/

func print(msg string) void {}


class Person {

	//Const attributes
	const name string = "Gabriel"
	const lastName string = "Igliozzi"
	
	//var attributes
	var age int = 19
	var isAllowed bool = true

	func augmentAgeBy(amt int) void {
        age = age + amt
	}

	func getFullName() string {
		return name + " " lastName
	}

}

//const gabriel Person = Person()
//print(gabriel.getFullName())

//Test statements
var count int = 0

while count < 100 && count > 90 {
	if count % 2 == 0 {
		print("even number" + count)
	}
	//count = count + 1
}

if 1 + 1 - 10 * -1 % 12.3 == 100 || !false == true {
	const result int = 1 + 1
	var index double = 1/2

	while result + 1 < index {
		var result int
	}
}

