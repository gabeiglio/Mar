//This file is to play a little bit with what I have of the compiler

//The idea is to reverse the string in the same variable
//so the input is also the output

var input string = "Gabriel Igliozzi"

//Lest say that we have a function that changes a character on a string at a specific index
func change(str string, c1 char, c2 char) void {}

//Reverse string

var fi int = 0  //First index character of the string input
var li int = 15 //Last index character of the string input

while fi != li {
	//change(input, input.at(fi), input.at(li))
	fi = fi + 1
	li = li - 1
}

func print(str string) void {
	print(str)
}
print(output)
